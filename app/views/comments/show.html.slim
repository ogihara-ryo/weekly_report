p#notice = notice

.panel.panel-default
  .panel-heading
    h3.panel-title
      = @comment.report
  .panel-body
    dl.dl-horizontal
      dt = t 'activerecord.attributes.comment.user'
      dd = @comment.user

      dt = t 'activerecord.attributes.comment.description'
      dd = @comment.description

= link_to '編集', edit_report_comment_path(@comment.report_id, @comment), class: 'btn btn-link'
'|
= link_to '戻る', report_path(@comment.report_id), class: 'btn btn-link'
