h1 Editing comment

== render 'form'

= link_to '詳細', report_comment_path(@comment.report_id, @comment), class: 'col-sm-offset-2 btn btn-link'
'|
= link_to '戻る', report_path(@comment.report_id), class: 'btn btn-link'

